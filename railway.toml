[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn bot:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"

[environments.production]
startCommand = "gunicorn bot:app --host 0.0.0.0 --port $PORT"